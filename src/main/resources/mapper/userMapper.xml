<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.readuu.mybatisdemo.mapper.UserMapper">
    <select id="findAll" resultType="User">
        select id,
               username,
               age
        from user
    </select>
</mapper>